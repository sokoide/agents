# X68000 入力デバイス — ハードウェアリファレンス

キーボード制御は MFP（MC68901）の USART、マウス制御は SCC（Z8530）、ジョイスティックは I/O コントローラが担当。

---

# 第10章 キーボード/マウス

## 10.1 キーボード/マウスの概要

キーボードとマウスインタフェースのブロック図を354ページの図1に示します。X68000は、キーボードとのデータ入出力をMFPのUSART（シリアルポート）、マウスからのデータ入力をSCCのBポートで行います。

キーボードとのデータ伝送は伝送速度2400bps、データ長8ビット、ストップビット1ビット、パリティなしです。マウスとのデータ伝送は伝送速度4800bps、データ長8ビット、ストップビット2ビット、パリティなしとなっています。

X68000では、キーボードや本体から専用ディスプレイの電源ON/OFFやTVのチャンネルの切り替えなどの制御が行えるようになっていますが、この制御はキーボード内のCPU（ワンチップマイコン：80C51）やシステムポート#2で行うようしています。キーボードの電源は本体の電源がOFFであっても供給され続けるVCC2からとっていますので、本体の電源がOFFであってもキーボード内のCPUは動作しており、キーボードによるTV制御が行えるようになっています。

また、X68000では本体とキーボードの両方にマウスコネクタがついていますが、この両方のデータ線は電気的つながっています。ただし、マウスデータ出力を要求するMSCTRL信号は、本体側はSCCのRTSB端子、キーボード側はキーボード上のCPUによって制御されるようになっています。キーボード側のMSCTRL信号の制御は、CPUへのコマンドによって行えるようになっています。

## 10.2 キーボード/マウス関連I/Oポート

キーボードとマウスの制御に関連するI/Oポートを図2にまとめてみました。これらのうち、MFPとSCCについては、それぞれのデバイスの説明のページを参照してください。

| アドレス | アクセス | ポート名 |
| :--- | :---| :--- |
| $E88027 | R/W | 同期キャラレジスタ |
| $E88029 | R/W | USART制御レジスタ |
| $E8802B | R/W | 受信ステータスレジスタ|
| $E8802D | R/W | 送信ステータスレジスタ|
| $E8802F | R/W | USARTデータレジスタ |
| $E8E003 | R/W | システムポート#2 |
| $E8E007 | R/W | システムポート#4 |
| $E98001 | R/W | SCCコマンドポート |
| $E98003 | R/W | SCCデータポート |

## 10.3 システムポート#2（$E8E003）

システムポート#2（アドレス：$E8E003）のビット配置を356ページの図3に示します。

```
bit 7     6     5     4     3     2     1     bit 0
[未使用 |未使用 |未使用 |未使用 |TV CTRL|未使用 | 3DスコープL | 3DスコープR ]
```

**ビット0-1：3Dスコープ制御/ステータス読み出し**

| 値 | 内容 |
|---|---|
| 11 | 左右ともCLOSE |
| 10 | 右のみOPEN |
| 01 | 左のみOPEN |
| 00 | 左右ともOPEN |

**ビット2：未使用**

**ビット3：TV CTRL（ディスプレイ制御）**

WRITE時：

- 1：ディスプレイ制御信号を'1'にする
- 0：ディスプレイ制御信号はキーボードからの制御信号となる（通常動作）

READ時：

- 1：ディスプレイの電源はOFF
- 0：ディスプレイの電源はON

ビット3の出力はキーボードからのディスプレイ制御信号とダイオードORされています。通常、キーボードからのディスプレイ制御信号は'0'になっているため、このビットを'1'にするとディスプレイ制御信号は'1'に、'0'にすれば'0'になります。これによって、キーボードがない状態でもソフトウェアでディスプレイ制御を行うことができます。

このビットに1を書いたままにしておくと、ディスプレイ制御信号は'1'に固定されたままとなるため、キーボードからの制御が行えなくなります。さらにディスプレイ内部では、この信号とワイヤレス（赤外線）リモコンからの信号がORされるようになっているらしく、リモコンによる制御も行えなくなります。通常はこのビットは'0'にするようにしてください。

**ビット4-7：未使用**

## 10.4 システムポート#4（$E8E007）

システムポート#4（アドレス：$E8E007）のビット配置を図4に示します。

```
bit 7     6     5     4     3       2        1      bit 0
[未使用 |未使用 |未使用 |未使用 |KEY CTRL|NMI RESET| HRL  |未使用 ]
```

**ビット0：未使用**

**ビット1：HRL（ドットクロック切り替え）**
通常は'0'に固定。

**ビット2：NMI RESET**

- 1：NMIリセットする
- 0：しない

**ビット3：KEY CTRL（キーデータ制御）**

WRITE時：

- 1：キーデータ送信可
- 0：キーデータ送信不可

READ時：

- 1：キージャック（キーボードコネクタ）が差し込まれている
- 0：キージャックは抜かれている

システムポート#4のビット3に'0'を書き込むと、RR信号が強制的に'1'（Lowレベル）にされ、キーボードはデータ送出が行えなくなります。キーボードはキーデータの送出を行った後で次のキースキャン（キーが押されたのか離されたのかをチェックする動作）を行いますので、この状態ではキーボードからのディスプレイ制御も行えなくなります。

通常、キーボードからデータが送られてくると、MFPはRR（Receiver Ready）信号を'1'（Lowレベル）にし、CPUがデータを読み取ると'0'（Highレベル）に復帰させます。キーボードはこの信号をチェックし、'0'になっているときだけキーデータを送るようにすることで、CPUがデータを引き取らないうちに次のデータを送ってしまうことを避けています。

このビットは読み出し時にはキーボードの挿抜ステータスとして機能します。キーボードが挿し込まれていると'1'、抜かれていると'0'になります。

**ビット4-7：未使用**

## 10.5 キーボードからの入力データ

キーボードから本体に送られてくるキーデータのフォーマットを358ページの図5に示します。キーデータはキーが押されたときと離されたときのいずれの場合も通知されます。下位7ビットで変化があったキーのキーコードが示され、ビット7で、そのキーが押されたのか離されたのかを示します。X68000の各キーの配置とキーコードの対応は358ページの図6のようになっています。

```
bit 7     6     5     4     3     2     1     bit 0
[ キーコード (7ビット)  |  S ]
```

**ビット7（S：ステータス）**

- 1：キーが離された
- 0：キーが押された

### キー配列とキーコード

キーコードは16進数で表記します。キー配列とキーコードの対応表は図6の通りです。

## 10.6 キーボードへの出力データ

X68000本体からキーボードへ与えるコマンドの一覧を図7に示します。X68000では、キーボード中のCPUがディスプレイ制御信号を発生したり、マウスコントロール信号（MSCTRL）の制御を行うようしているため、それらの機能をサポートするためのコマンドが多くなっています。

```
bit 7     6     5     4     3     2     1     bit 0
[  |  |  |  |  |  |  |  ]
```

| コマンド | 内容 |
|---------|------|
| TV CTRL Code | 専用ディスプレイ（ディスプレイTV）制御 |
| LED CTRL | キーボード上のLEDの点灯/消灯制御 |
| MSCTRL | キーボードのマウスコネクタのMSCTRL信号制御 |
| DISPLAY CTRL | ディスプレイコントロール |

## 10.7 ディスプレイコントロール

X68000では、キーボードの操作によるディスプレイ制御だけでなく、本体からキーボードのCPUに対してディスプレイ制御信号の発生を要求することができるようしています。このためのコマンドの一覧を360ページの図8に示します。

このコマンドでは、電源のON/OFFや、ノーマルコントラストでのスーパーインポーズなど、キーボードからの操作ではできないものも含まれています。とくにノーマルコントラストでのスーパーインポーズは、通常のスーパーインポーズ時よりもTV画面が明るくなりますので、スーパーインポーズのときは、このモードを利用したほうがよいでしょう。

### ディスプレイコントロールコマンド一覧

| キー | SHIFTキーなし | SHIFTキーあり |
|------|--------------|--------------|
| Vol. up | 音量（ボリューム）up | - |
| Vol. down | 音量down | - |
| Vol. normal | ノーマル | - |
| Call | チャンネルコール | - |
| CS down | テレビ画面（初期化、リセット） | - |
| Mute | 音声ミュート | - |
| CH16 | 無効 | - |
| BR up | テレビ/コンピュータ画面切り替え（トグル） | - |
| BR down | テレビ/外入力切り替え（トグル） | - |
| - | コンピュータ画面 | - |
| - | CH up | チャンネルUP |
| - | CH down | チャンネルdown |
| - | 無効 | - |
| - | Power ON/OFF | 電源ON/OFF（トグル） |
| - | スーパーインポーズON/OFF（トグル）、コントラストダウン | - |

## 10.8 マウスコントロール信号制御

コマンドのフォーマットを360ページの図9に示します。ビット0でキーボードについているマウスコネクタのMSCTRL信号の状態を選択します。

```
bit 7     6     5     4     3     2     1     bit 0
[  |  |  |  |  |  |  | MSCTRL ]
```

- 1：MSCTRLを'High'する
- 0：MSCTRLを'Low'する

マウスは、MSCTRLがHighからLowなったのをとらえてデータの送出を開始します。

## 10.9 キーデータ送出許可/禁止

CPUがキーデータを引き取らなかったり、システムポートでキーボードのキーデータの送出を禁止したりすると、キーボードはキーデータの送出が行えるようになるまでキースキャンを停止してしまうため、キーボードによるディスプレイ制御も行えなくなります。

このような状態を避けるため、キーボードに対してキーデータを送出せずキースキャン動作を行わせるようするのが、このコマンドです。このコマンドでキーデータの送出を禁止すると、キーボードのCPUはキーデータを本体に送出するのをやめますが、ディスプレイ制御信号の発生は行いますので、キーボードによるディスプレイ制御は通常どおり行うことができます。

コマンドフォーマットを図10に示しています。最下位ビットを'0'にするとキーデータ送出が禁止され、'1'にすると通常の動作モードになります。

### キーデータ送出許可/禁止

```
bit 7     6     5     4     3     2     1     bit 0
[  |  |  |  |  |  |  | DATA ]
```

- 1：キーデータ送出許可
- 0：キーデータ送出禁止

## 10.10 ディスプレイコントロールキーモード

キー操作によるディスプレイ制御を、X1とコンパチブルなモードにするか否かを選択します。コマンドのフォーマットを図11に示しています。

```
bit 7     6     5     4     3     2     1     bit 0
[  |  |  |  |  |  |  | MODE ]
```

- 1：X68000モード
- 0：X1コンパチモード

通常のモード（X68000モードと呼ぶこともあります）とX1コンパチモードの違いを図12に示します。X68000モードでは、スーパーインポーズや入力の切り替えが、データを送るたびにトグル（交互切り替わり）しますが、X1コンパチモードでは、キー入力によってスーパーインポーズ、TV、コンピュータの選択になります。

## 10.11 LED明るさ選択

キーボード上のLEDの明るさの選択を行います。コマンドフォーマットを図13に示しています。下位2ビットでLEDの明るさを4段階調整できます。

```
bit 7     6     5     4     3     2     1     bit 0
[  |  |  |  |  |  |BRIGHTNESS ]
```

**キーボード上のLEDの明るさ**

| 値 | 内容 |
|---|---|
| 11 | 暗い |
| 10 | やや暗い |
| 01 | やや明るい |
| 00 | 明るい |

## 10.12 本体からのディスプレイ制御の有効/無効選択

本体からキーボードに要求するディスプレイ制御コマンドを受け付けるか否かを選択します。コマンドフォーマットを図14に示しており、最下位ビット（ビット0）を'0'にすると、制御コマンドが無効になります。

```
bit 7     6     5     4     3     2     1     bit 0
[  |  |  |  |  |  |  | ENABLE ]
```

- 1：本体からのディスプレイ制御有効
- 0：本体からのディスプレイ制御無効

## 10.13 OPT.2キーよるディスプレイ制御許可/禁止

コマンドのフォーマットを図15に示しています。キー操作でのディスプレイ制御は通常SHIFTキーを用いますが、OPT.2をSHIFTキーの代用として使うこともできるようになっています。このコマンドは、このOPT.2キーよるディスプレイ制御を許可するか、禁止するかを選択するものです。

```
bit 7     6     5     4     3     2     1     bit 0
[  |  |  |  |  |  |  | ENABLE ]
```

- 1：OPT.2キーよるディスプレイ制御許可
- 0：OPT.2キーよるディスプレイ制御禁止

## 10.14 キーリピート開始時間設定

コマンドのフォーマットを図16に示しています。キーを押し続けたとき、キーリピートが開始されるまでの時間を設定します。

下位4ビットによって、リピート開始までの時間を200msから1700msまで100ms単位で設定することができます。キーボードがリセットされたときは、この時間は500msに初期設定されます。

```
bit 7     6     5     4     3     2     1     bit 0
[  |  |  |  | REP. DELAY (4ビット) ]
```

キーリピート開始までの時間 = 200 + (REP. DELAY) × 100 (ms) で算出される
（リセット時は500ms）

## 10.15 キーリピート間隔設定

コマンドのフォーマットを図17に示しています。キーリピートの間隔を30ms〜1155msの間で設定することができます。キーボードがリセットされたときは、この間隔は110msに設定されます。

```
bit 7     6     5     4     3     2     1     bit 0
[ REP. TIME (8ビット) ]
```

キーリピート間隔 = 30 + (REP. TIME) × 5 (ms) で算出される
（リセット時は110ms）

## 10.16 キーボードLED制御

キーボードに付いているLED付きのキーのLED点灯/消灯を制御します。コマンドフォーマットを図18に示しています。下位7ビットがそれぞれのキーに対応しており、ビットが'1'のとき消灯、'0'のとき点灯します。

```
bit 7     6     5     4     3     2     1     bit 0
[  | LED6 | LED5 | LED4 | LED3 | LED2 | LED1 | LED0 ]
```

各キーのLEDの点灯/消灯制御

- 1：消灯
- 0：点灯

## 10.17 ディスプレイ制御信号

キーボードからのディスプレイ制御信号がきていないときや、キーボードコネクタが抜けているときは、システムポート#2のビット3を制御してディスプレイ制御信号をつくり出せば、本体だけでもディスプレイ制御を行うことができます。

ディスプレイ制御信号のフォーマットは366ページの図19のようになっています。1回のデータ送出は48msの間隔をおいて表信号と裏信号を送ることで行われます。表信号と裏信号のデータの内容を図20に示します。裏信号は基本的には表信号の反転データであり、ディスプレイ側ではこの両方が正しく受け取れたことを確認してからコマンドを実行することでノイズ等による誤動作を防いでいるわけです。

### ディスプレイコントロール信号

```
|    48 ms    |    48 ms    |
0 ───────────────────────────────────────
ディスプレイ              表信号              裏信号
コントロール信号                              電信号

1 ───────────────────────────────────────
各ビットの波形     | |       |  |
'0'
```

### ディスプレイへの送出データ

ディスプレイに送るデータ（左側のビットから順に送出される）：

| 信号 | C0 | C1 | C2 | C3 | C4 | C5 | C6 | C7 | C8 | C9 | C10 | C11 |
|------|----|----|----|----|----|----|----|----|----|----|----|-----|
| 表 | '0' | '0' | ディスプレイコントロールコード | そのまま |
| 裏 | '0' | '0' | ディスプレイコントロールコードの反転 | そのまま |

ディスプレイ制御信号の各ビットの情報は、単純データの1/0を信号の1/0に対応させているのではなく、250μs幅のノーパルスの後、次のパルスまで何msの間隔をあけるかということで表すようしています。

これらの方法はワイヤレスリモコンで行われている方法です。ディスプレイ内では、ワイヤレスリモコンから受け取った信号と本体から送られてくる信号が単純ORされてリモコンデータとなっているようです。

## 10.18 キーボードの特殊機能

本来の用途とはあまり関係ありませんが、キーボードが持っているおまけ的な機能を紹介しておきましょう。

### LEDの明るさ指定

キーボードをリセットするとき（キーボードを抜き挿しするとき）にLEDの明るさ選択が行えます。

| 起動条件 | LED明るさ |
|---------|----------|
| 何も押さないで立ち上げたとき | 明るい |
| XF3を押しながら立ち上げたとき | やや明るい |
| XF4を押しながら立ち上げたとき | やや暗い |
| XF5を押しながら立ち上げたとき | 暗い |

### LEDチェック

F1、F2、F3の3つのキーを同時押しながらキーボードをリセットすると、LEDが点滅を繰り返します。この状態では、キーボードからの入力などはまったく行えませんので、使用するときはキーを押さない状態で再度キーボードリセットを行ってください。

## 10.19 マウス制御

マウスは、マウス制御信号（信号名：MSCTRL）がHighからLowに変化すると、ステータス、X方向データ、Y方向データの3バイトデータを送ってきます。マウス制御タイミングの規定などを368ページの図21に示します。

マウスデータ信号（信号名：MSDATA）は本体のマウスコネクタとキーボードからのものが単純接続されているだけですが、MSCTRL信号は本体側はSCC、キーボード側はキーボード内のCPUで制御されるため、マウスがどちらに接続されていてもよいかどうかするためには、SCCとキーボードの両方でMSCTRL信号を操作する必要があります。

マウスから送られてくるX方向、Y方向のデータは符号付き2進数で、$80が-128、$7Fが+127を示します。このデータは前回データを送りはじめた時点からの相対的な移動量を示します。

### マウスのデータ転送タイミング

```
500μs以上                    700μs以上
500μs 以上

伝送速度：4800bps

START | DATA (8ビット) | STOP (2ビット)
(1ビット)
```

### マウスのステータスデータ

```
bit 7     6     5     4     3     2     1     bit 0
[ YOFL | YUFL | XOFL | XUFL |  |  | RSW | LSW ]
```

| ビット | 内容 |
|-------|------|
| 7 | Y方向オーバフロー（Y方向の移動量が+128以上） |
| 6 | Y方向アンダフロー（Y方向の移動量が-129以下） |
| 5 | X方向オーバフロー（X方向の移動量が+128以上） |
| 4 | X方向アンダフロー（X方向の移動量が-129以下） |
| 3 | 未使用 |
| 2 | 未使用 |
| 1 | 右スイッチ（1: ON、0: OFF） |
| 0 | 左スイッチ（1: ON、0: OFF） |

ビット1とビット0は、マウスの左右のスイッチの状態を示します。'1'のときスイッチが押されていることを、'0'のとき離れていることを示します。
